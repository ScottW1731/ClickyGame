import React from "react";

const Directions = () => {
    return(
        <div>
            <h1>TotalWar Icon Clicky Game</h1>
            <h3>Click on each image no more than once, click all 12 to win!</h3>
        </div>
    );

};

export default Directions;